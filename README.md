# Sitio Web Pliage realizado en ReactJS

En la carpeta build se encuentra los archivos compilados para la publicacion web

para la utilizacion del proyecto es necesario instalar los paquetes requiridos.

` yarn install`
